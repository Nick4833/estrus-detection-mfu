# Results / reproducibility artifacts

Generate the reproducibility log yourself, in your own environment, so it reflects a
real run rather than a copied value:

```bash
python ../compute_cis.py | tee reproducibility_log.txt
```

